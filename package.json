{
  "scripts": {
    "start": "npm run clean && npm run build && node ./dist/main.js",
    "build": "tsc --build",
    "clean": "rm -rf ./dist"
  },
  "dependencies": {
    "@types/puppeteer": "^5.4.4",
    "puppeteer": "^13.0.1"
  },
  "devDependencies": {
    "express": "^4.17.2",
    "morgan": "^1.10.0",
    "typescript": "^4.5.4"
  }
}
